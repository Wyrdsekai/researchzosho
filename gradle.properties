# The signing key for Maven Central publication (the same key as hermod). The public half is on
# keyserver.ubuntu.com and keys.openpgp.org. Only the key NAME lives here: no passphrase, no private
# material; the gpg agent supplies those at publish.
signing.gnupg.keyName=125077B534F00DFE
