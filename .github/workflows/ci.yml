name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: test (${{ matrix.os }}, JDK ${{ matrix.java }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # 21 is the floor the code targets; 25 is what a current install gives you. Both, because an
        # exact toolchain pin once meant a machine with only the newer JDK could not build at all.
        include:
          - os: ubuntu-latest
            java: '21'
          - os: ubuntu-latest
            java: '25'
          - os: macos-latest
            java: '25'
    steps:
      - uses: actions/checkout@v7
      # Only GitHub-owned actions: the Wyrdsekai org allows no third-party actions. setup-java's own
      # cache keeps the Gradle wrapper and dependencies between runs; gradle/actions is not needed.
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
          cache: gradle
      - run: ./gradlew :librarian:test :client:test --console=plain
